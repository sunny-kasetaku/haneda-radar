name: Run Haneda Radar

# ▼ 書き込み権限を強制的に付与（これでPushエラーも消えます）
permissions:
  contents: write

# ▼ ボタン(workflow_dispatch) と 保存時(push) の両方で動く設定
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      AVIATION_STACK_API_KEY: ${{ secrets.AVIATION_STACK_API_KEY }}
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install requests

      - name: Run script
        run: python main_v8.py

      - name: Commit and Push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add index.html
          git commit -m "Update flight data" || exit 0
          git push

      - name: Show URL
        run: echo "🚀 Access here: https://sunny-kasetaku.github.io/haneda-radar/"