name: Haneda Radar Auto Update

on:
#  schedule:
#    - cron: '*/20 * * * *'
  workflow_dispatch:        # 手動実行ボタン
  repository_dispatch:      # GASからのトリガー
    types: [trigger-radar]

# 古い実行を自動キャンセル
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10      # ブラウザを動かすため少し余裕を見て10分に延長

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' # Playwrightが安定して動くよう少しバージョンを上げました

      - name: Install dependencies and Playwright
        run: |
          python -m pip install --upgrade pip
          pip install playwright
          # ブラウザ本体と、それを動かすためのシステムライブラリをインストール
          playwright install chromium
          playwright install-deps chromium

      - name: Run Haneda Radar
        run: python haneda_radar.py

      - name: Commit and Push changes
        run: |
          git config --local user.name "GitHub Action"
          git config --local user.email "action@github.com"
          git add index.html analysis_result.json
          if git diff --staged --quiet; then
            echo "No changes"
          else
            git commit -m "Auto update Haneda stats: $(date +'%Y-%m-%d %H:%M:%S')"
            git pull --rebase origin main
            git push
          fi

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
