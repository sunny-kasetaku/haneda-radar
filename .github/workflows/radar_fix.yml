name: Haneda Radar Final

on:
  # 手動実行ボタン（テスト用）
  workflow_dispatch:
  
  # 👇【変更】ここをコメントアウトして、更新時の自動起動を停止しました
  # push:
  #   branches: [ main ]

  # 👇【追加】毎時3分に自動実行（これを入れないと自動更新されません）
  schedule:
    - cron: '3 * * * *'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      AVIATION_STACK_API_KEY: ${{ secrets.AVIATION_STACK_API_KEY }}
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
      DISCORD_THREAD_URL: ${{ secrets.DISCORD_THREAD_URL }}

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - run: pip install requests
      - run: python main_v8.py
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4